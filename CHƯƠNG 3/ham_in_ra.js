// 3 hàm hiễn thị ra trình duyệt cơ bản

const str = 'hello world';
alert(str); // tạo hộp thông báo đơn giản

const str1 = prompt("mời bạn nhập vaod một giá trị str1 = ?"); // tạo 1 hộp form input để nhập dữ liệu cơ bản
console.log(str1); 

const str3 = confirm("bạn đang có người yêu phải không"); // tạo hôp thông báo in ra giá trị value theo true và false
console.log(str3);


let day = prompt('Mời bạn chọn tháng');
let day1 = parseInt(day);

 switch (day1) {
     case (1) :
        case (2) :
            case (3) :
                case (4) :

         alert(`thang ${day1} có 30 ngày`);
 }